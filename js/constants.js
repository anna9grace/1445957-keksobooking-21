'use strict';

(function () {
  const MAX_CAPACITY = 100;

  const accomodationType = {
    palace: `Дворец`,
    flat: `Квартира`,
    house: `Дом`,
    bungalow: `Бунгало`,
  };

  window.constants = {
    MAX_CAPACITY,
    accomodationType,
  };
})();
